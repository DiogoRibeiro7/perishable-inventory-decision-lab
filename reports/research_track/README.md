# Research Track Artifacts

This directory documents the advanced probabilistic comparison surface. Full experiment outputs are generated locally with:

```bash
poetry run perishable-lab research-comparison input.csv --output-dir artifacts/research-comparison
```

No generated large experiment output is committed here. The decision rule is documented in `docs/ADVANCED_PROBABILISTIC_RESEARCH.md`.
