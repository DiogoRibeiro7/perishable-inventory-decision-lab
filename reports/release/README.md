# Release Reports

This directory stores compact release-gate evidence files.

Generated run outputs remain under `artifacts/` and are intentionally excluded from version control. The committed report files here should be small enough for code review and should point back to reproducible commands.
