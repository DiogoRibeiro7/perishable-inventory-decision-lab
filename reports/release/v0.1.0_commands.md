# v0.1.0 Command Evidence

Environment:

- OS: Windows 11
- Python used by Poetry: 3.12.11
- Package version: 0.1.0

Commands:

| Command | Result |
|---|---|
| `poetry run perishable-lab demo --days 90 --stores 2 --products 4 --seed 42 --output-dir artifacts/release-demo-check` | Completed. Monitoring status: `healthy`; test rows: `104`; empirical coverage: `0.894`; best cost per demand unit in this run: fixed service level at `0.360`. |
| `poetry run python scripts/release_gate.py --write reports/release/v0.1.0_gate.json` | Completed. Status: `pass`; 30 required paths present; local Markdown links resolve; cards match package version `0.1.0`. |
| `poetry run ruff check .` | Completed. `All checks passed!` |
| `poetry run mypy src` | Completed. `Success: no issues found in 57 source files` |
| `poetry run pytest` | Completed. `225 passed in 14.07s`; total package coverage: `89%`. |
| `poetry run python scripts/repo_hygiene.py` | Completed. `Repository hygiene checks passed.` |
| Public wording scan | Completed. No matches. |

The shell initially reported Python 3.13.5 as outside the project range and Poetry selected Python 3.12.11 automatically.
